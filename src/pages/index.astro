---
// Esta página reimplementaba a mano el <html>, el <head>, el navbar móvil y el
// drawer completos, en paralelo a BaseLayout. El coste no era teórico: la home
// —la URL que más se comparte del sitio— se quedó sin una sola etiqueta
// Open Graph, porque esas viven en BaseHead y aquí nunca se importó. Al pasar
// por BaseLayout las hereda, y además entra en ViewTransitions como el resto.
//
// El título y la descripción no cambian: BaseLayout ya usa SITE_TITLE y
// SITE_DESCRIPTION como valores por defecto, que es exactamente lo que este
// head declaraba a mano.
import BaseLayout from "../layouts/BaseLayout.astro";
import HeroSection from "../components/HeroSection.astro";
import ReferencesSection from "../components/ReferencesSection.astro";
import EsquemaFlujo from "../components/EsquemaFlujo.astro";
import HomeContenido from "../components/HomeContenido.astro";

// Las secciones ponen su propio ancho y su propio padding, así que el <main>
// sólo tiene que dejarlas correr de borde a borde.
const mainClass = "w-full";

// `proximity`, no `mandatory`: alinea la sección cuando ya vas llegando a su
// borde, pero si paras a media altura para leer te deja ahí. El snap anterior
// era `mandatory` y además vivía en un contenedor con `overflow-y-auto`, que es
// lo que rompía el botón atrás y los anclajes entre páginas; aquí va sobre el
// documento, así que no reintroduce ninguna de las dos cosas.
//
// `scroll-pt-16` descuenta el navbar sticky de 4rem que hay en móvil: sin él la
// cabecera de cada sección aterrizaba por debajo de la barra, tanto al hacer
// snap como al clicar la flecha. En `lg` la barra no existe y el descuento se
// anula.
const htmlClass = "snap-y snap-proximity scroll-pt-16 lg:scroll-pt-0";
---

<BaseLayout sideBarActiveItemID="home" mainClass={mainClass} htmlClass={htmlClass}>
  <!-- Sin scroll-snap. Estaba puesto sólo en `lg:`, así que en móvil nunca
       existió, y en desktop sujetaba menos de lo que parecía: al medirlas, tres
       de las cuatro secciones ya son más altas que la ventana de un portátil,
       de modo que el snap no alineaba pantallas — peleaba con el scroll interno
       del contenido que no cabía.
       Lo que costaba era real: el scroll ocurría en un contenedor con
       `overflow-y-auto`, y el navegador sólo restaura la posición del documento.
       Volver atrás desde el blog te dejaba arriba, y un enlace a /#casos desde
       otra página no saltaba. Ahora el scroll es del documento y las dos cosas
       funcionan. -->

  <!-- El hero sí conserva la pantalla completa: es lo primero que se ve y
       quieres que ocupe todo. En móvil descuenta los 4rem del navbar sticky. -->
  <section
    id="hero"
    class="relative flex min-h-[calc(100dvh-4rem)] snap-start items-center justify-center px-6 py-8 lg:min-h-screen lg:py-0"
  >
    <HeroSection />
    <div
      id="scroll-indicator"
      class="absolute inset-x-0 bottom-6 flex justify-center text-2xl text-gray-500 transition-opacity duration-500 animate-bounce motion-reduce:animate-none"
    >
      <a href="#contenido" aria-label="Desliza hacia abajo">↓</a>
    </div>
  </section>

  <!-- Las otras tres van con espaciado vertical fijo en vez de `min-h-screen`.
       Con el alto atado a la ventana, el ritmo cambiaba según la pantalla:
       en un portátil el contenido se pasaba de largo y en un monitor grande
       sobraba aire. Un padding constante se lee igual en las dos. -->
  <section id="contenido" class="snap-start px-6 py-16 lg:py-24">
    <div class="mx-auto w-full max-w-4xl">
      <EsquemaFlujo />
    </div>
  </section>

  <section id="casos" class="snap-start px-6 py-16 lg:py-24">
    <div class="mx-auto w-full max-w-4xl">
      <!-- `sectionClass=""` para no sumar el `py-16` propio del componente al
           padding que ya pone la sección. -->
      <ReferencesSection showCta={false} sectionClass="" />
    </div>
  </section>

  <!-- Sin `snap-start` a propósito: es la última sección, mide más de una
       pantalla y detrás va el pie. Su punto de snap cae a sólo ~200 px del
       final del scroll, así que alinearla pelearía con llegar al pie —el bug
       clásico de "no puedo bajar del todo". El final de la página se deja
       libre. -->
  <section id="decidir" class="px-6 py-16 lg:py-24">
    <div class="mx-auto w-full max-w-4xl">
      <HomeContenido />
    </div>
  </section>
</BaseLayout>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const indicator = document.getElementById("scroll-indicator");
    const target = document.getElementById("contenido");

    if (!indicator || !target) return;

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          const hidden = entry.isIntersecting;
          indicator.style.opacity = hidden ? "0" : "1";
          // Con sólo la opacidad a 0, la flecha seguía siendo clicable y
          // enfocable con Tab estando invisible.
          indicator.classList.toggle("pointer-events-none", hidden);
          indicator.setAttribute("aria-hidden", String(hidden));
        });
      },
      { threshold: 0.1 }
    );

    observer.observe(target);
  });
</script>
